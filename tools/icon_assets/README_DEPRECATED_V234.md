# Legacy art (not active)

This is the historical v2.3.1 icon source. VQEAF OS v2.3.4 uses
`tools/pixel_icon_assets/png/*.png` and `src/core/VqeafIconData.h` instead.
`tools/rebuild_vqeaf_icons.py` was redirected to the new compiler. Do not copy
the older `firmware/VqeafIconData.h` into `src/core/` during future releases.
